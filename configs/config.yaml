# It contains all the configs required in the project

artifacts:
  ARTIFACTS_DIR: artifacts
  PREPARED_DATA: prepared
  TRAIN_DATA: train.tsv
  TEST_DATA: test.tsv
  FEATURIZED_DATA: featured
  FEATURIZED_TRAIN_DATA: train.pkl
  FEATURIZED_TEST_DATA: test.pkl
  MODEL_DIR: model
  MODEL_NAME: model.pkl
  

source_data_url: https://dvc-aiops-nlp.s3.amazonaws.com/data.xml
# source_data: C:\\Downloads\\data.xml

source_download_dirs:
  data_dir: data
  data_file: data.xml
